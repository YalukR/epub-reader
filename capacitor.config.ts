import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'epub-reader',
  webDir: 'dist/epub-reader/browser'
};

export default config;
